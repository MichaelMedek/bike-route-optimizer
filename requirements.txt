# Dependencies live here ONLY (single source of truth). setup.py reads this file
# into install_requires; pyproject.toml holds tooling config, not dependencies.

# Install the package itself (editable), so `bike_router` is importable in CI/dev.
-e .

# Geocoding
geopy>=2.4.0

# Geospatial processing
shapely>=2.0.0
rasterio>=1.3.0
pyproj>=3.6.0

# OSM graph + routing
osmnx>=2.0.0
networkx>=3.0
scikit-learn>=1.3.0  # osmnx.distance.nearest_nodes needs it for unprojected graphs
pyrosm>=0.6.0  # fast local .osm.pbf reader for the offline country-graph builder
geopandas>=0.14.0  # GeoDataFrames from pyrosm; node/edge tables
pyarrow>=14.0.0  # tiled GeoParquet artifact read/write

# GPX + numerics + visualization
gpxpy>=1.6.0
numpy>=1.24.0
matplotlib>=3.7.0
tqdm>=4.60.0  # CLI progress bar

# Web viewer (Streamlit 3D map). Uses the built-in st.pydeck_chart — no
# streamlit-deckgl needed (no click/flythrough interaction).
streamlit>=1.36.0
pydeck>=0.9.0

# DEM hosting / paths
huggingface_hub>=0.20.0
requests>=2.28.0

# Testing + tooling
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.9.0
mypy>=1.13.0
types-requests>=2.28.0
pre-commit>=3.5.0
