# Dependencies live here ONLY (single source of truth). setup.py reads this file
# into install_requires; pyproject.toml holds tooling config, not dependencies.

# Geocoding
geopy>=2.4.0

# Geospatial processing
shapely>=2.0.0
rasterio>=1.3.0
pyproj>=3.6.0

# OSM graph + routing
osmnx>=2.0.0
networkx>=3.0
scikit-learn>=1.3.0  # osmnx.distance.nearest_nodes needs it for unprojected graphs

# GPX + numerics + visualization
gpxpy>=1.6.0
numpy>=1.24.0
matplotlib>=3.7.0

# DEM hosting / paths
huggingface_hub>=0.20.0
platformdirs>=3.0.0
requests>=2.28.0

# Testing + tooling
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.9.0
mypy>=1.13.0
pre-commit>=3.5.0
