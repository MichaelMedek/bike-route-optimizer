# RUNTIME dependencies ONLY (inference + web UI). The dataset-preprocessing stack
# (osmnx/pyrosm/geopandas/rasterio/…) lives in requirements-preprocessing.txt, which
# adds on top of this file. setup.py reads this into install_requires (single source).
# Each line notes whether CORE (inference) or UI (web app) needs it.

# Install the package itself (editable), so `bike_router` is importable in CI/dev.
-e .

# Geocoding
geopy>=2.4.0  # CORE: Nominatim geocode of place strings

# Geospatial + routing
shapely>=2.0.0  # CORE: corridor polygons + edge geometry
scipy>=1.11.0  # CORE: sparse CSR Dijkstra routing engine
pyarrow>=14.0.0  # CORE: pandas parquet engine, pushdown filters
numpy>=1.24.0  # CORE: vectorized cost + haversine math
pandas>=2.0.0  # CORE: node/edge tables, vectorized cost

# GPX + debug PNG
gpxpy>=1.6.0  # CORE: write the route GPX
matplotlib>=3.7.0  # CORE: headless debug route PNG
tqdm>=4.60.0  # CORE: CLI download progress bar
psutil>=5.9.0  # CORE: process RSS memory-guard logging

# Web viewer (Streamlit 3D map). st_deckgl (not the built-in st.pydeck_chart) because st.pydeck_chart
# keeps the panned camera, so "Set start & end" could never re-zoom.
streamlit>=1.36.0  # UI: the web app shell
pydeck>=0.9.0  # UI: deck.gl terrain + route layers
streamlit-deckgl>=0.5.0  # UI: remountable deck component, re-zoom
plotly>=5.18.0  # UI: interactive elevation-profile chart
altair>=5.0.0  # UI: composition + grade donuts

# Artifact hosting / paths
huggingface_hub>=0.20.0  # CORE: download prebuilt graph artifact
requests>=2.28.0  # CORE: Photon autocomplete HTTP calls

# Testing + tooling
pytest>=8.0.0  # DEV: test runner
pytest-cov>=5.0.0  # DEV: coverage gate measurement
ruff>=0.9.0  # DEV: lint, import-sort, format
mypy>=1.13.0  # DEV: static type checking
types-requests>=2.28.0  # DEV: requests stubs for mypy
pre-commit>=3.5.0  # DEV: run gates pre-commit
docstring-parser>=0.16  # DEV: validate terse Google-style docstrings in the strictness test
