# DATASET-PREPROCESSING dependencies — the offline build stack, ADDED ON TOP of the
# runtime requirements.txt. Install with:  pip install -r requirements-preprocessing.txt
-r requirements.txt

# OSM read + graph construction
osmnx>=2.0.0  # BUILD: projection + intersection consolidation
networkx>=3.0  # BUILD: the build-time MultiDiGraph representation
pyrosm>=0.6.0  # BUILD: fast local .osm.pbf reader
geopandas>=0.14.0  # BUILD: GeoDataFrames from pyrosm ways
scikit-learn>=1.3.0  # BUILD: osmnx nearest_nodes on unprojected graphs
pyproj>=3.6.0  # BUILD: UTM projection for metric consolidation

# DEM elevation sampling
rasterio>=1.3.0  # BUILD: read cropped EuroDEM, sample elevations
